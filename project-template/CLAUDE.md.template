# [项目名称] — QA Context

## 技术栈
- 前端框架：[Next.js 14 / Vue 3 / React 等]
- 包管理器：pnpm
- 测试工具：Playwright（E2E）、Vitest + Testing Library（单元测试）
- Preview URL：见 .env → PREVIEW_URL

## 项目结构
- 源码目录：src/
- PRD 文档：docs/prd/
- 组件路径：src/components/
- API 路径：src/api/ 或 src/services/

## 测试约定
- AI 生成脚本存放在 tests/*/generated/（不得手动修改）
- 用例先生成 Markdown，再导出 Excel
- Bug 上报到 Linear，Project ID 见 .env → LINEAR_PROJECT_ID
- data-testid 命名规范：{模块}-{组件}-{功能}，例如 auth-login-submit

## 业务背景（帮助 AI 理解领域）
[简短描述项目是做什么的，方便 AI 生成更准确的测试用例]

## 已安装 Plugin
- qa-platform（测试自动化）

## 可用命令
- /qa-explore：探查浏览器页面，生成 E2E 测试基线 + 用例 + POM + spec
- /qa-from-issue：从 Linear issue 生成或更新 E2E 测试
- /qa-run-all：运行 E2E 测试，汇总报告，上报 Linear
- /qa-run-prd：PRD 驱动 E2E 测试流水线
- /qa-report-bugs：手动触发 Bug 上报
